# Dependencies — reinstalled inside the image
node_modules

# Build output — rebuilt inside the image
.next
out

# Local env files — secrets must be passed as build ARGs or runtime env vars
.env.local
.env.*.local

# Version control
.git
.gitignore

# Editor / OS noise
.DS_Store
*.swp
*.swo

# Claude Code
.claude

# Logs
*.log
npm-debug.log*
yarn-debug.log*
bun-error.log*
